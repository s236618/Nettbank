@model Nettbank.Model.User

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Slett</title>
</head>
<body>
    <br /><br />
    <h3>Er du sikker på at du vil slette denne?</h3>
    <div>
        <h4>Kunde</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.persNr)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.persNr)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Slett" class="btn btn-danger" />
                @Html.ActionLink("Kundeliste", "UserList", new { tom = "" }, new { @class = "btn btn-primary" })
            </div>
        }
    </div>
</body>
</html>
